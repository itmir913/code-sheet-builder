# CLAUDE.md

이 저장소에서 작업할 때 지켜야 할 규칙이다. 사람이 읽어도 되지만, 일차 독자는 이 저장소를 처음 여는 에이전트다.

## 이 저장소가 무엇인가

코드에 빈칸을 뚫어 인쇄용 코딩 학습지를 만드는 단일 페이지 웹 앱이다. 서버가 없고, 데이터는 브라우저 밖으로 나가지 않는다. 배포 대상은 둘이다 - GitHub Pages 사이트와, 압축을 풀어 `index.html` 을 더블클릭해 쓰는 오프라인 zip.

`teacher-utility-kit` 의 하위 폴더였다가 커밋 이력째 분리해 나온 저장소다. `score-reporter` 저장소가 같은 방식으로 먼저 분리됐고, 빌드·CI·배포 구성은 그쪽과 의도적으로 같은 모양을 하고 있다. 한쪽에서 고친 구성이 다른 쪽에도 맞는지 한 번 생각해 볼 것.

## 명령은 셋뿐이다

```bash
npm run dev      # 개발 서버
npm run build    # dist/ 생성
npm run ci       # eslint → vitest → vite build
```

**이 셋 외에 스크립트를 늘리지 않는다.** `lint`, `test`, `preview` 같은 항목을 `package.json` 에 따로 두고 싶어지겠지만 두지 않는다. 명령이 늘면 "CI가 실제로 무엇을 돌리는가"가 흩어지고, 로컬에서 친 명령과 CI가 친 명령이 달라진다. 린트만 돌리고 싶으면 `npx eslint .` 처럼 직접 부르면 된다.

CI 워크플로도 `npm run ci` 하나만 부른다. 스텝을 쪼개 `npm run lint` / `npm test` 로 나누지 말 것.

**검사 명령은 세 곳에서 같아야 한다** - 로컬에서 치는 `npm run ci`, `.idea/runConfigurations/ci.xml` 의 실행 구성, 두 워크플로의 검사 스텝. 한 곳만 바꾸면 "내 쪽에서는 통과했는데 CI가 떨어진다"가 시작된다.

워크플로에는 `npm ci` 스텝이 따로 있는데 이건 npm 의 클린 설치 명령이지 위 스크립트가 아니다. 러너에는 `node_modules` 가 없어서 필요한 환경 준비 단계이고, 로컬에서는 lockfile 이 바뀔 때만 직접 돌리면 된다. 이름이 닮았다고 중복으로 보지 말 것.

작업을 끝내기 전에 `npm run ci` 가 통과하는지 반드시 확인한다.

## 배포는 사람이 누를 때만 일어난다

`.github/workflows/deploy-pages.yml` 은 `workflow_dispatch:` 하나만 트리거로 갖는다. push로 배포되지 않는다. **이건 의도된 것이니 `on: push` 를 더하지 말 것.**

Actions 탭에서 실행하면 한 번의 실행 안에서 세 가지가 순서대로 일어난다.

1. `npm run ci` 로 검사하고 `dist/` 를 만든다 (실패하면 여기서 멈춘다)
2. 그 `dist/` 를 GitHub Pages에 올린다
3. 같은 `dist/` 를 압축한 `code-sheet-builder.zip` 을 `latest` 릴리스에 갱신한다

`release` 잡이 `deploy` 를 기다리는 것도 의도다. 사이트는 실패했는데 zip만 새것이 되는 상황을 막는다.

push와 PR에는 `ci.yml` 만 돌고, 배포는 하지 않는다.

### 워크플로가 Actions 탭에 안 보일 때

`workflow_dispatch:` 만 트리거로 갖는 워크플로는 **파일을 추가한 그 push 에서는 등록되지 않는 때가 있다.** 저장소를 처음 만들고 push 했을 때 실제로 그랬다 - `ci.yml` 은 push 가 그 워크플로를 실제로 돌렸으니 바로 등록됐지만, 아무것도 트리거하지 않은 `deploy-pages.yml` 은 파일만 올라간 채 목록에 뜨지 않았고 `gh workflow run` 이 404 를 냈다. 몇 분을 기다려도 마찬가지였다.

**그 워크플로 파일 자체를 고친 커밋**을 push 하면 등록된다. GitHub 은 워크플로 파일의 블롭이 바뀐 push 에서 워크플로를 등록하기 때문이다 - 다른 파일만 바꾼 push 는 아무 소용이 없다(그것도 해 봤다). 파일이 잘못된 게 아니니 YAML 을 고치려 들지 말 것.

## 오프라인 zip이 모든 제약의 이유다

zip을 풀어 `index.html` 을 더블클릭하면 주소가 `file://` 이 된다. 브라우저는 이 출처를 `null` 로 보기 때문에 평소에 되던 것들이 막힌다. 그래서:

- **실행 시점에 네트워크를 부르지 않는다.** CDN 링크, Google Fonts `<link>`, 원격 이미지를 넣지 말 것. 폰트와 라이브러리는 npm으로 받아 번들에 넣는다.
- **`<script type="module">` 로 나가면 안 된다.** `vite.config.js` 의 `fileProtocolSafeHtml` 플러그인이 빌드 결과에서 `type="module"` 과 `crossorigin` 을 걷어낸다.
- **번들은 iife 한 덩어리다.** 코드 분할을 켜면 `file://` 에서 청크를 가져오지 못한다.
- **경로는 상대 경로다** (`base: './'`). 절대 경로면 Pages 하위 경로나 `file://` 중 하나가 깨진다.

빌드 설정을 건드릴 일이 생기면 `vite.config.js` 주석을 먼저 읽을 것. 각 옵션이 왜 그 값인지 적혀 있다.

바꾼 뒤에는 `npm run build` 하고 `dist/index.html` 을 실제로 더블클릭해 열어 확인한다. 개발 서버에서 되는 것과 `file://` 에서 되는 것은 다르다.

## 마스크 오프셋과 줄바꿈

가려진 영역은 코드 문자열의 문자 오프셋(`start`, `end`)으로 저장된다. 코드에 `\r\n` 이 섞이면 오프셋이 줄마다 한 칸씩 밀려 엉뚱한 글자가 가려진다. 눈에 잘 안 띄고 저장 파일에 그대로 굳는 종류의 버그다.

- 코드가 상태로 들어가는 길목(`UPDATE_BLOCK_CODE`, `LOAD_STATE`)에서 `\r\n` → `\n` 정규화를 유지할 것.
- `.gitattributes` 의 `eol=lf` 를 풀지 말 것.
- 오프셋을 다루는 코드(`mask.service.js` 의 `mapHtmlToRaw`, `print.js` 의 줄 단위 분할)를 고치면 경계값 테스트를 함께 고칠 것.

## 테스트

`tests/` 에 Vitest로 둔다. DOM이 필요하면 jsdom 환경이 이미 켜져 있다.

테스트를 붙일 값어치가 있는 곳은 정해져 있다 - 상태 리듀서, 마스크 오프셋 계산, 인쇄용 렌더링. 셋 다 순수 함수에 가깝고, 틀렸을 때 화면만 봐서는 알기 어렵다. 반대로 Monaco 인스턴스 생성이나 DOM 이벤트 배선은 테스트로 잡기 어렵고 값어치도 적다 - 그건 브라우저에서 직접 확인한다.

리듀서나 오프셋 계산을 고칠 때는 테스트를 함께 고친다.

## 코드에 대해

- 모듈은 ES 모듈이다. 전역에 무언가를 올리지 않는다 (`window.X = ...` 금지).
- 문자열로 HTML을 만드는 곳이 많다. 사용자 입력이 들어가는 자리에는 반드시 `esc()` 를 거칠 것.
- 상태 변경은 전부 `Store.dispatch` 를 지난다. 상태 객체를 바깥에서 직접 고치지 말 것.
- 주석은 "무엇을"이 아니라 "왜"를 적는다. 코드를 보면 아는 것을 다시 쓰지 않는다.
- 기존 파일의 서식(4칸 들여쓰기, 한글 주석)을 따른다.

## 커밋

- **커밋 메시지에 AI 관련 문구를 넣지 않는다.** `Co-Authored-By: Claude ...`, `Generated with Claude Code`, `🤖` 같은 줄을 붙이지 말 것. PR 본문도 마찬가지다. 이 저장소의 이력은 사람이 쓴 것으로 남긴다.
- 메시지는 한국어로, 무엇을 했는지 한 줄로 적는다. 기존 이력의 말투를 따를 것 (`~한다`, `~수정`).
- 커밋과 push는 사용자가 요청할 때만 한다.

## 손대지 말 것

- `teacher-utility-kit/code-sheet-builder/` - 원본 저장소에 아직 남아 있는 폴더다. 그쪽은 별도로 정리할 예정이니 이 작업 중에 건드리지 않는다.

## 바이너리는 저장소에 두지 않는다

예제코드 zip(`CodeSheet_Example_Code.zip`)은 `latest` 릴리스 자산으로만 배포한다. 저장소 이력에서도 지웠다 - 바이너리는 델타 압축이 듣지 않아 한 번 들어가면 clone 비용으로 영구히 남는다.

`index.html` 의 '예제코드' 버튼은 그 릴리스 URL 을 가리킨다. 이 파일을 다시 커밋하지 말 것. 내용을 바꿀 일이 생기면 `gh release upload latest <파일> --clobber` 로 자산만 갈아 끼운다.

이 때문에 오프라인 zip 에서 네트워크가 필요한 곳은 릴리스를 가리키는 두 링크뿐이다 -
'예제코드' 와 '오프라인 버전'. 둘 다 사용자가 눌러야 발생하므로 화면이 뜨는 것 자체는
네트워크 없이 된다. 나머지는 전부 번들 안에 있다.
